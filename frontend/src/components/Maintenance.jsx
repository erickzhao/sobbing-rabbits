import React from 'react';
import { Layout } from 'antd';

const Maintenance = props => {
  return <Layout style={{ paddingTop: 100, minHeight: '100vh' }}>Maintenance</Layout>;
};

export default Maintenance;
